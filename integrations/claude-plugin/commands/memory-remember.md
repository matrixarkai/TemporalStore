---
description: Persist an important fact, preference, or decision to MatrixArk long-term memory.
argument-hint: "[fact to remember]"
---

Use the `matrixark` MCP write/ingest tool to store the following as durable, time-aware memory:

$ARGUMENTS

Store it as a typed memory (fact / preference / decision / entity state) with the current
session scope. Confirm exactly what was stored and under which scope, so it can be recalled later.
