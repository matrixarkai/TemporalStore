---
description: Recall relevant time-aware long-term memory from MatrixArk (TemporalStore) for the current task.
argument-hint: "[what to recall]"
---

Use the `matrixark` MCP tools to retrieve a ranked, time-aware ContextPack relevant to:

$ARGUMENTS

Prefer the retrieval / context-pack tool. Then briefly summarize what was recalled
(memories, entities, prior decisions) and cite the source refs so the user can trace them.
If nothing relevant is found, say so instead of guessing.
